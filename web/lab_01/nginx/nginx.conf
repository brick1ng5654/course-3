events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;  # ← Важно!
    default_type  application/octet-stream;
    
    server {
        listen 80;
        root /usr/share/nginx/html;
        index index.html;
        
        location / {
            try_files $uri $uri/ /index.html;
        }
    }
}


